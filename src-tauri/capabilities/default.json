{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permessi di base per la finestra principale del Rotellario.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
